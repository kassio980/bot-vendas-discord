const {info}=require('../../embeds');
module.exports={id:'cli_produtos',async execute(c,i){
await i.update({embeds:[info('⚙️ cli_produtos','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
