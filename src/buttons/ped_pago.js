const {info}=require('../../embeds');
module.exports={id:'ped_pago',async execute(c,i){
await i.update({embeds:[info('⚙️ ped_pago','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
