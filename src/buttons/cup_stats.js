const {info}=require('../../embeds');
module.exports={id:'cup_stats',async execute(c,i){
await i.update({embeds:[info('⚙️ cup_stats','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
