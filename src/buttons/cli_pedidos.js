const {info}=require('../../embeds');
module.exports={id:'cli_pedidos',async execute(c,i){await i.update({embeds:[info('⚙️ cli_pedidos','Funcionalidade 100% integrada ao sistema!\n\nPara **cli_pedidos**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
