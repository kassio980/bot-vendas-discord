const {info}=require('../../embeds');
module.exports={id:'cli_pedidos',async execute(c,i){
await i.update({embeds:[info('⚙️ cli_pedidos','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
