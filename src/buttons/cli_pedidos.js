const {info}=require('../../embeds');
module.exports={id:'cli_pedidos',async execute(c,i){
await i.update({embeds:[info('⚙️ cli_pedidos','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
