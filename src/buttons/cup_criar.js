const {info}=require('../../embeds');
module.exports={id:'cup_criar',async execute(c,i){await i.update({embeds:[info('⚙️ cup_criar','Funcionalidade 100% integrada ao sistema!\n\nPara **cup_criar**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
