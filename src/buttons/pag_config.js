const {info}=require('../../embeds');
module.exports={id:'pag_config',async execute(c,i){
await i.update({embeds:[info('⚙️ pag_config','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
