const {info}=require('../../embeds');
module.exports={id:'pag_config',async execute(c,i){
await i.update({embeds:[info('⚙️ pag_config','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
