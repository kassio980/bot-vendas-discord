const {info}=require('../../embeds');
module.exports={id:'produto_lista',async execute(c,i){
await i.update({embeds:[info('⚙️ produto_lista','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
