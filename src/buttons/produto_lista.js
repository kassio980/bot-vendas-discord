const {info}=require('../../embeds');
module.exports={id:'produto_lista',async execute(c,i){
await i.update({embeds:[info('⚙️ produto_lista','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
