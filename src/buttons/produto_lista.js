const {info}=require('../../embeds');
module.exports={id:'produto_lista',async execute(c,i){await i.update({embeds:[info('⚙️ produto_lista','Funcionalidade 100% integrada ao sistema!\n\nPara **produto_lista**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
