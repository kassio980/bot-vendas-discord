const {info}=require('../../embeds');
module.exports={id:'adm_entregas',async execute(c,i){
await i.update({embeds:[info('⚙️ adm_entregas','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
