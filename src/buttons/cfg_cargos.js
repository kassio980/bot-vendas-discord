const {info}=require('../../embeds');
module.exports={id:'cfg_cargos',async execute(c,i){
await i.update({embeds:[info('⚙️ cfg_cargos','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
