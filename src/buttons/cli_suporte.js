const {info}=require('../../embeds');
module.exports={id:'cli_suporte',async execute(c,i){
await i.update({embeds:[info('⚙️ cli_suporte','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
