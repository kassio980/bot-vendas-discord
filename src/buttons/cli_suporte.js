const {info}=require('../../embeds');
module.exports={id:'cli_suporte',async execute(c,i){
await i.update({embeds:[info('⚙️ cli_suporte','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
