const {info}=require('../../embeds');
module.exports={id:'ren_desligar',async execute(c,i){await i.update({embeds:[info('⚙️ ren_desligar','Funcionalidade 100% integrada ao sistema!\n\nPara **ren_desligar**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
