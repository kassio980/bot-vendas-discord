const {info}=require('../../embeds');
module.exports={id:'ent_criar',async execute(c,i){
await i.update({embeds:[info('⚙️ ent_criar','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
