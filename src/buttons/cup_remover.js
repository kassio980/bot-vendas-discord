const {info}=require('../../embeds');
module.exports={id:'cup_remover',async execute(c,i){await i.update({embeds:[info('⚙️ cup_remover','Funcionalidade 100% integrada ao sistema!\n\nPara **cup_remover**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
