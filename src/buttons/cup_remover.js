const {info}=require('../../embeds');
module.exports={id:'cup_remover',async execute(c,i){
await i.update({embeds:[info('⚙️ cup_remover','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
