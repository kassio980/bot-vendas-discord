const {info}=require('../../embeds');
module.exports={id:'pag_testar',async execute(c,i){
await i.update({embeds:[info('⚙️ pag_testar','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
