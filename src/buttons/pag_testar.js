const {info}=require('../../embeds');
module.exports={id:'pag_testar',async execute(c,i){
await i.update({embeds:[info('⚙️ pag_testar','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
