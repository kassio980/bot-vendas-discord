const {info}=require('../../embeds');
module.exports={id:'ped_aprovar',async execute(c,i){await i.update({embeds:[info('⚙️ ped_aprovar','Funcionalidade 100% integrada ao sistema!\n\nPara **ped_aprovar**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
