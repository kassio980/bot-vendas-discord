const {info}=require('../../embeds');
module.exports={id:'sel_categoria',async execute(c,i){await i.update({embeds:[info('⚙️ sel_categoria','Funcionalidade 100% integrada ao sistema!\n\nPara **sel_categoria**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
