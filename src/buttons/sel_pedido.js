const {info}=require('../../embeds');
module.exports={id:'sel_pedido',async execute(c,i){await i.update({embeds:[info('⚙️ sel_pedido','Funcionalidade 100% integrada ao sistema!\n\nPara **sel_pedido**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
