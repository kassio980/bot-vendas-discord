const {info}=require('../../embeds');
module.exports={id:'sel_pedido',async execute(c,i){
await i.update({embeds:[info('⚙️ sel_pedido','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
