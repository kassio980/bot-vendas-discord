const {info}=require('../../embeds');
module.exports={id:'adm_cupons',async execute(c,i){await i.update({embeds:[info('⚙️ adm_cupons','Funcionalidade 100% integrada ao sistema!\n\nPara **adm_cupons**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
