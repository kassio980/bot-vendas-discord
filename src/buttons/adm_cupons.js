const {info}=require('../../embeds');
module.exports={id:'adm_cupons',async execute(c,i){
await i.update({embeds:[info('⚙️ adm_cupons','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
