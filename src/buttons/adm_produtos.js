const {info}=require('../../embeds');
module.exports={id:'adm_produtos',async execute(c,i){
await i.update({embeds:[info('⚙️ adm_produtos','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
