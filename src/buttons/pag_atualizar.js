const {info}=require('../../embeds');
module.exports={id:'pag_atualizar',async execute(c,i){
await i.update({embeds:[info('⚙️ pag_atualizar','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
