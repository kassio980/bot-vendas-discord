const {info}=require('../../embeds');
module.exports={id:'pag_atualizar',async execute(c,i){
await i.update({embeds:[info('⚙️ pag_atualizar','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
