const {info}=require('../../embeds');
module.exports={id:'recusar_pagamento',async execute(c,i){
await i.update({embeds:[info('⚙️ recusar_pagamento','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
