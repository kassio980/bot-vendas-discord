const {info}=require('../../embeds');
module.exports={id:'ren_status',async execute(c,i){await i.update({embeds:[info('⚙️ ren_status','Funcionalidade 100% integrada ao sistema!\n\nPara **ren_status**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
