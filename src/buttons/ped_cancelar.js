const {info}=require('../../embeds');
module.exports={id:'ped_cancelar',async execute(c,i){
await i.update({embeds:[info('⚙️ ped_cancelar','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
