const {info}=require('../../embeds');
module.exports={id:'log_pagamentos',async execute(c,i){
await i.update({embeds:[info('⚙️ log_pagamentos','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
