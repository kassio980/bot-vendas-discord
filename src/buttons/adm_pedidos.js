const {info}=require('../../embeds');
module.exports={id:'adm_pedidos',async execute(c,i){
await i.update({embeds:[info('⚙️ adm_pedidos','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
