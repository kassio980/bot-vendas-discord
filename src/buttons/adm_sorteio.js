const {info}=require('../../embeds');
module.exports={id:'adm_sorteio',async execute(c,i){await i.update({embeds:[info('⚙️ adm_sorteio','Funcionalidade 100% integrada ao sistema!\n\nPara **adm_sorteio**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
