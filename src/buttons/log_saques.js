const {info}=require('../../embeds');
module.exports={id:'log_saques',async execute(c,i){
await i.update({embeds:[info('⚙️ log_saques','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
