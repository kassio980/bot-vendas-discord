const {info}=require('../../embeds');
module.exports={id:'log_saques',async execute(c,i){
await i.update({embeds:[info('⚙️ log_saques','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
