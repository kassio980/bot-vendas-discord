const {info}=require('../../embeds');
module.exports={id:'produto_editar',async execute(c,i){
await i.update({embeds:[info('⚙️ produto_editar','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
