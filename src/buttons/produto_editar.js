const {info}=require('../../embeds');
module.exports={id:'produto_editar',async execute(c,i){await i.update({embeds:[info('⚙️ produto_editar','Funcionalidade 100% integrada ao sistema!\n\nPara **produto_editar**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
