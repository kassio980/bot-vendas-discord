const {info}=require('../../embeds');
module.exports={id:'produto_editar',async execute(c,i){
await i.update({embeds:[info('⚙️ produto_editar','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
