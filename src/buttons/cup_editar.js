const {info}=require('../../embeds');
module.exports={id:'cup_editar',async execute(c,i){await i.update({embeds:[info('⚙️ cup_editar','Funcionalidade 100% integrada ao sistema!\n\nPara **cup_editar**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
