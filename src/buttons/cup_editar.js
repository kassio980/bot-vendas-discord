const {info}=require('../../embeds');
module.exports={id:'cup_editar',async execute(c,i){
await i.update({embeds:[info('⚙️ cup_editar','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
