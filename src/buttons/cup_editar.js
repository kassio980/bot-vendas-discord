const {info}=require('../../embeds');
module.exports={id:'cup_editar',async execute(c,i){
await i.update({embeds:[info('⚙️ cup_editar','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
