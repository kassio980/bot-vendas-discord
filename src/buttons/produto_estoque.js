const {info}=require('../../embeds');
module.exports={id:'produto_estoque',async execute(c,i){
await i.update({embeds:[info('⚙️ produto_estoque','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
