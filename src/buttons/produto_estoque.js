const {info}=require('../../embeds');
module.exports={id:'produto_estoque',async execute(c,i){await i.update({embeds:[info('⚙️ produto_estoque','Funcionalidade 100% integrada ao sistema!\n\nPara **produto_estoque**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
