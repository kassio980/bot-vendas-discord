const {info}=require('../../embeds');
module.exports={id:'produto_estoque',async execute(c,i){
await i.update({embeds:[info('⚙️ produto_estoque','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
