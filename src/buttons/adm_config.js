const {info}=require('../../embeds');
module.exports={id:'adm_config',async execute(c,i){
await i.update({embeds:[info('⚙️ adm_config','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
