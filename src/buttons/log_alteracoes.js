const {info}=require('../../embeds');
module.exports={id:'log_alteracoes',async execute(c,i){
await i.update({embeds:[info('⚙️ log_alteracoes','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
