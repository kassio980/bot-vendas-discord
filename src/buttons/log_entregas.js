const {info}=require('../../embeds');
module.exports={id:'log_entregas',async execute(c,i){
await i.update({embeds:[info('⚙️ log_entregas','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
