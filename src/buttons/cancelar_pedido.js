const {info}=require('../../embeds');
module.exports={id:'cancelar_pedido',async execute(c,i){
await i.update({embeds:[info('⚙️ cancelar_pedido','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
