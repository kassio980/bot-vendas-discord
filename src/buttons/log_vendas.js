const {info}=require('../../embeds');
module.exports={id:'log_vendas',async execute(c,i){
await i.update({embeds:[info('⚙️ log_vendas','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
