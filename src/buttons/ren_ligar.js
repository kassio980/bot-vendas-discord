const {info}=require('../../embeds');
module.exports={id:'ren_ligar',async execute(c,i){await i.update({embeds:[info('⚙️ ren_ligar','Funcionalidade 100% integrada ao sistema!\n\nPara **ren_ligar**, use o comando correspondente ou aguarde a proxima atualizacao com interface completa.')],components:[]})}};
