const {info}=require('../../embeds');
module.exports={id:'ent_estoque',async execute(c,i){
await i.update({embeds:[info('⚙️ ent_estoque','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
