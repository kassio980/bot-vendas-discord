const {info}=require('../../embeds');
module.exports={id:'cfg_msgs',async execute(c,i){
await i.update({embeds:[info('⚙️ cfg_msgs','Funcionalidade em implementacao. Em breve disponivel!')],components:[]})}};
