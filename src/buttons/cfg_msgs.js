const {info}=require('../../embeds');
module.exports={id:'cfg_msgs',async execute(c,i){
await i.update({embeds:[info('⚙️ cfg_msgs','Funcionalidade em liberacao. Em breve 100% operacional!')],components:[]})}};
